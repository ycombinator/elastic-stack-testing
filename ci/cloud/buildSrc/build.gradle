repositories {
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    compile group: 'co.elastic.cloud', name: 'sdk-java', version: "1.2.0-SNAPSHOT"
    compile group: 'com.bettercloud', name: 'vault-java-driver', version: '3.1.0'
}